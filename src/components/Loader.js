import React from 'react';
import LinearProgress from '@mui/material/LinearProgress';

const Loader = () => {
    return (
        <div>
            <LinearProgress />
        </div>
    );
};

Loader.propTypes = {};

export default Loader;